# Homework: Javascript II

## Instrucciones
---
1. En un archivo de texto separado que debes crear, escribe explicaciones de los siguientes conceptos
 como si se lo estuvieras explicando a un niño de 12 años. 
 Hacer esto te ayudará a descubrir rápidamente cualquier agujero en tu comprensión.

* `for`
* `&&`, `||`, `!`

* For es una sentencia que se utiliza para realizar bucles repetitivos. En sus argumentos se declara
 una variable y se la inicializa, se pone la condición que es evaluada en cada iteracion y es la uqe determina 
 si el bucle continua o para; y como ultimo argumento el incremento o decremento de la variable que 
 se utiliza para evaluar la condicion del bucle. 

 *Son operadores booleanos, que dan como resultado true o false.
 && es el operador and (todas las condiciones deben ser verdaderas para que el resultado sea verdadero)
 || es el operador operador (con una condicion verdadera, el resutado ya es verdadero)
 ! es el operador de negacion (invierte el valor booleano !true es false !false es true)